# Copy to .env beside fleet.yaml and fill in. Never commit the real one —
# fleet.yaml references the key by name so the secret stays here.
#
# One key for the whole fleet: the SPINLOOP_API_TOKEN of every node's daemon,
# and the key the behind-docker engine is gated with.
FLEET_TOKEN=
