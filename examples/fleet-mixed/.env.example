# Copy to .env beside fleet.yaml and fill in. Never commit the real one —
# fleet.yaml references these by name so the secrets stay here.
#
# Only the daemon node needs a token here. The remote environments sign each
# call with your AWS credentials, so they need nothing in this file.
#
# GPU_BOX_TOKEN is the SPINLOOP_API_TOKEN of that machine's daemon.
GPU_BOX_TOKEN=
