version: 2

project_name: spinloop

builds:
  - id: spinloop
    binary: spinloop
    main: ./cmd/spinloop
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: default
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

brews:
  - name: spinloop
    # Publish the formula to the dedicated tap repository.
    repository:
      owner: spinloop-ai
      name: homebrew-tap
      branch: main
      # Uses a PAT with write access to the tap repo (the default GITHUB_TOKEN
      # only has access to this repository, not the tap).
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/spinloop-ai/spinloop"
    description: "Configure opencode provider catalogues"
    license: "MIT"
    # Publish into Formula/ — Homebrew ignores root-level .rb files once a tap
    # has a Formula directory (the shared tap already has one for other casks).
    directory: Formula
    # Skip publishing the formula for pre-release tags.
    skip_upload: auto
    commit_author:
      name: goreleaser-bot
      email: bot@goreleaser.com
    # Install the completions the binary prints, so `brew install` wires TAB up
    # with no extra step. (PowerShell is supported too, but not by Homebrew.)
    extra_install: |
      generate_completions_from_executable(bin/"spinloop", "completion", shells: [:bash, :zsh])
    test: |
      system "#{bin}/spinloop", "--help"

checksum:
  name_template: checksums.txt

changelog:
  use: git
  sort: asc
  groups:
    - title: Features
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: Other changes
      order: 999
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^style:"

release:
  draft: false
  prerelease: auto
  # The daemon's control API contract, attached per release so a consumer can
  # pin it to the spinloop version it talks to rather than reading whatever is
  # currently on the default branch. Kept honest by
  # internal/daemon/openapi_test.go.
  extra_files:
    - glob: docs/openapi.yaml
